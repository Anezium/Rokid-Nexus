<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.anezium.rokidbus.glasses.permission.START_SELFARM_WIRELESS"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <!-- Photo sync reads the native camera's captures from /sdcard/DCIM/Camera. This app targets
         API 32, so READ_EXTERNAL_STORAGE is still the media-read permission (the READ_MEDIA_*
         split lands at API 33) and it is requested at runtime from MainActivity. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <queries>
        <package android:name="com.rokid.os.sprite.assistserver" />
        <intent>
            <action android:name="com.rokid.os.sprite.tts.TTS_SERVICE" />
        </intent>
        <intent>
            <action android:name="com.anezium.rokidbus.action.CLIENT" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RokidBus.Glasses">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Started by the phone over CXR to begin setup. Exported because it arrives from
             another process; it only opens a session and the public Accessibility screen, so the
             worst a rogue caller achieves is showing the user a Settings page they can dismiss. -->
        <activity
            android:name=".SetupEntryActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <activity
            android:name=".SurfaceActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RokidBus.Surface" />

        <activity
            android:name=".CameraActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:excludeFromRecents="true"
            android:exported="false"
            android:process=":camera"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RokidBus.Surface" />

        <activity
            android:name=".VideoActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:process=":video"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RokidBus.Surface" />

        <service
            android:name=".RokidBusAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/rokidbus_accessibility_service" />
        </service>

        <!-- This IME intentionally draws no keyboard on the AR display. Once selected by the
             wearer, it keeps Android's InputConnection available for the paired phone UI. -->
        <service
            android:name=".NexusRemoteInputMethodService"
            android:exported="true"
            android:label="@string/remote_input_method_label"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/nexus_remote_input_method" />
        </service>

        <service
            android:name=".BusHubService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.anezium.rokidbus.action.HUB" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".CameraOverlayVisibilityReceiver"
            android:exported="false" />

        <receiver
            android:name=".SelfArmWirelessBootstrapReceiver"
            android:exported="false"
            android:permission="com.anezium.rokidbus.glasses.permission.START_SELFARM_WIRELESS">
            <intent-filter>
                <action android:name="com.anezium.rokidbus.glasses.ACTION_SELFARM_WIRELESS_START" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".OpenLauncherReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.anezium.rokidbus.glasses.action.OPEN_LAUNCHER" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
