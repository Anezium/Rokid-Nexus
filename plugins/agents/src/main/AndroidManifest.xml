<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
        POST_NOTIFICATIONS is deliberately absent. Agents alerts belong on the
        glasses, never in the phone's shade. Without the permission Android keeps
        the monitoring foreground service running but never shows its notification,
        which is exactly the behaviour we want.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!--
        The other half of the bargain above. A foreground service whose
        notification is never shown is not treated as a protected one on every
        ROM: measured on a Vivo device, Android froze this process outright
        (cgroup.freeze=1, no CPU time at all) while the service still reported
        isForeground=true. The kernel kept completing TCP handshakes from its
        backlog, so the daemon saw connections that never answered hello and
        redialled forever. The exemption is what stops that, and the settings
        screen asks for it rather than assuming it.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <queries>
        <intent>
            <action android:name="com.anezium.rokidbus.action.HUB" />
        </intent>
        <!-- Package visibility so the away-from-home setup can tell whether
             Tailscale is already on this phone. -->
        <package android:name="com.tailscale.ipn" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_agents"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agents"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".AgentsSettingsActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".AddComputerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ComputerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ProjectPickerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".NewThreadActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".AgentsMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="agent-session-monitoring" />
        </service>

        <service
            android:name=".AgentsPluginService"
            android:exported="true"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="glasses-session" />
            <intent-filter>
                <action android:name="com.anezium.rokidbus.action.PLUGIN" />
            </intent-filter>
            <meta-data
                android:name="com.anezium.rokidbus.plugin.ID"
                android:value="agents" />
            <meta-data
                android:name="com.anezium.rokidbus.plugin.DISPLAY_NAME"
                android:value="Agents" />
            <meta-data
                android:name="com.anezium.rokidbus.plugin.ICON"
                android:value="terminal" />
            <meta-data
                android:name="com.anezium.rokidbus.plugin.API_VERSION"
                android:value="3" />
            <meta-data
                android:name="com.anezium.rokidbus.plugin.CAPABILITIES"
                android:value="surfaces" />
            <meta-data
                android:name="com.anezium.rokidbus.plugin.RECEIVE_PREFIXES"
                android:value="/plugin/agents,/system/plugin" />
            <meta-data
                android:name="com.anezium.rokidbus.plugin.SETTINGS_ACTIVITY"
                android:value=".AgentsSettingsActivity" />
            <meta-data
                android:name="com.anezium.rokidbus.plugin.LAUNCHABLE"
                android:value="true" />
        </service>
    </application>
</manifest>
